# Scripts

`check_docs.py` checks repository documentation using Python’s standard library. From the repository root, run:

```bash
python3 scripts/check_docs.py
```

GitHub Actions runs the same check. See [contributing](../CONTRIBUTING.md) for its coverage and limits.

Azure CLI and PowerShell automation will be added after it is reviewed and exercised in a lab. No Azure deployment scripts have been added yet.
